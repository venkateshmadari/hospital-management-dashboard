export type TripleBreadCrumbsTypes = {
  firstLink: string;
  firstName: string;
  secondLink: string;
  secondName: string;
  thirdName: string;
};
export type DoubleBreadCrumbsTypes = {
  firstLink: string;
  firstName: string;
  secondName: any;
  isLoading?: boolean;
};

export type SingleBreadCrumbsTypes = {
  name: string;
};
